version: '3.8'
services:
  myorg-web:
    container_name: myorg-web
    image: myorg-web:latest
    build:
      context: ./
      target: production
    environment:
      GOOGLE_CLIENT_ID: 86598659yu
    restart: always
    ports:
      - 3000:3000
    volumes:
      - ./:/app